/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  reactStrictMode: true,
  images: {
      domains: ["https://lh3.googleusercontent.com"],
      formats: ["image/webp"],
  },
};